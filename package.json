{
  "name": "dao-delegates",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service show-karma/dao-delegates",
    "create-local": "graph create --node http://localhost:8020/ dao-delegates",
    "remove-local": "graph remove --node http://localhost:8020/ dao-delegates",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dao-delegates"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
