{
  "name": "token-delegates",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ token-delegates",
    "create-local": "graph create --node http://localhost:8020/ token-delegates",
    "remove-local": "graph remove --node http://localhost:8020/ token-delegates",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 token-delegates"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
